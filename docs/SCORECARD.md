# Withheld internal scorecard

Snapshot: 2026-09-02. This is an internal 0–5 assessment, not an official judging result and
not a Tier A claim. Scores reflect the evidence class in the last column. A combined
`VERIFIED_* + UNKNOWN` status means the local design/run is real, while the requested external
proof is still open.

| ID | Score | Current evidence | Design strength | Missing evidence | Failure condition | Next action | Evidence class |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| W1 Essentiality | 4.6 | Page-owned arithmetic/redaction in source; GATE-W1; 19 native checks | Same-page state is materially safer than an external grader | Hosted model transcript and counterfactual proof | Numeric, point, pass, identity, or release authority crosses the boundary | Run hosted model replay and rerun W1 | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W2 Tool quality | 4.6 | Nine closed tools, bounds, annotations, runtime validation, native registry | Narrow tools expose useful recognition operations without a release tool | Hosted registry capture and final-commit binding | Extra fields, oversized payloads, or invalid IDs are silently accepted | Capture the same registry on the authorized hosted URL | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W3 Context/authority | 4.5 | Revision guard, monotonic emphasis, atomic proposals, receipts, recovery trace | The page remains the authority for context and state transitions | Model-driven stale recovery and hosted receipt capture | Stale write overwrites current state or a human event is absent | Complete hosted/model stale-retry journey | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W4 Shared state | 4.5 | UI and tools share `SessionPort`; browser trace observes DOM/revision/receipts | Agent work and human review converge on one visible state | Hosted DOM capture after native/model calls | Tool result comes from a mock or separate state | Capture hosted before/after screenshots and payloads | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W5 Safety/recovery | 4.0 | Injection quarantine, invalid/unknown/oversized/stale/duplicate refusals, no confirm tool, 27-check recovery | Refusal is bounded and recovery is actionable | Model behavior under the same refusals | Leak, agent release, hidden recovery, or unhandled exception | Run model safety repetitions and inspect refusal payloads | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X1 Complete loop | 4.2 | Local read → propose → hold → stage → human decline/confirm → reload trace | The narrow journey is resettable and fully page-owned | Hosted journey and sub-three-minute video | A judge must assemble the key sequence or final event is missing | Record authorized hosted journey/video | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X2 Correctness | 4.5 | 125 assertions; pure marks/session tests; browser and recovery assertions | Arithmetic and revision invariants are deterministic | Clean-clone and Node 22/CI evidence | Revision, mark, receipt, or release state diverges | Run fresh install and CI on a writable Node 22/24 runner | `VERIFIED_RUN` + `VERIFIED_ARTIFACT` + `ENVIRONMENT_BLOCKED` |
| X3 UX/demo clarity | 4.1 | Monochrome three-column UI; marked/staged views; visible refusal and human gate | Agent role and human boundary are inspectable | Non-builder comprehension and final video | Dense first viewport or failure path is not understood | Run GATE-P2 protocol and record the judge path | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X4 Responsiveness | 3.7 | 1440/1487/420 screenshots, focus/CSP/console and responsive checks | Static local app has a useful no-agent fallback | Controlled latency, long-task, memory, hosted/mobile/manual checks | Tool/UI hangs, overflow, failed asset, or unexplained request | Run performance probe on representative hardware and hosted URL | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X5 Reproducibility | 3.6 | Package scripts, MIT license, docs, source/build hashes | A reviewer can reproduce the local prototype | Clean public repository, final commit, hosted URL, CI, video | Local-only instructions or hashes do not identify the release | Publish only with permission, then create final manifest | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| I1 Problem clarity | 3.6 | Short-answer marking problem and bounded fixture are documented | The problem is concrete rather than generic “AI for education” | GATE-P2 answers from a non-builder | Persona/problem is only author assertion or impact is overstated | Run the six-question GATE-P2 protocol | `SELF_REPORTED` + `UNKNOWN` |
| I2 Audience specificity | 3.2 | Teacher/marker/reviewer wording and synthetic aliases | The intended workflow owner is understandable | Independent workflow-owner validation | No one outside the builder recognizes the use case | Recruit one eligible non-builder and preserve verbatim answers | `SELF_REPORTED` + `UNKNOWN` |
| I3 Before/after | 3.2 | Preview outcome and held subset show the intended state difference | Recognition can reduce unattended work while preserving review | Measured or observed workflow comparison with a non-builder | No demonstrable difference from ordinary manual marking | Run a bounded before/after comprehension session; do not invent time savings | `VERIFIED_SOURCE` + `UNKNOWN` |
| I4 Human-agent split | 3.8 | Agent recognises/proposes; page computes/holds; human-only confirm; receipts | Authority is explicit and technically enforced | Natural-language model transcript and human validation | Agent computes marks, releases, or bypasses hold | Complete model replay and GATE-P2 | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| I5 Credibility/trust | 3.7 | Synthetic disclosure, no backend/network/PII, security and limitation docs | Claims are deliberately narrow and auditable | Independent review, hosted security headers, final package | Synthetic data presented as adoption or injection mitigation claimed as general | Obtain GATE-P2 and hosted header evidence; retain limitations | `VERIFIED_SOURCE` + `SELF_REPORTED` + `UNKNOWN` |
| C1 Original mechanic | 4.5 | Information boundary, redacted payload, page arithmetic, human release | The memorable idea is a capability boundary, not a chatbot skin | Public/video side-by-side demonstration | It looks like a generic AI grading dashboard | Show one refusal beside the page-owned arithmetic in the video | `VERIFIED_SOURCE` + `UNKNOWN` |
| C2 Human-agent relationship | 4.4 | “Recognizer, not grader” role, narrow tools, absent confirm tool | The agent has a precise seat and a deliberate refusal | Model-selected replay | Agent is framed as an autonomous grader or publisher | Replay natural-language requests and preserve tool selection | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| C3 WebMCP novelty | 4.3 | Native registry and page-local state/permission checks | WebMCP is used for same-page context, not as decoration | Hosted native/model counterfactual | A conventional external API provides the same experience | Capture hosted native call plus counterfactual narration | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| C4 Differentiation | 4.3 | Distinct marking/release thesis and bounded static scope | The project has a clear domain and does not need feature sprawl | Public presentation and final submission context | It is confused with another workspace submission or benchmark | Use the clean repository and explicit submission copy when authorized | `SELF_REPORTED` + `VERIFIED_SOURCE` + `UNKNOWN` |
| C5 Ambition/scope | 4.0 | Deep trust-boundary design completed in a small prototype; recovery exists | Scope is ambitious in safety while remaining buildable | Hosted/model/P2/package completion | Speculative backend/features displace evidence closure | Finish external gates before adding scope | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |

## Aggregate and target check

| Dimension | Mean | Internal target | Result |
| --- | ---: | ---: | --- |
| WebMCP Leverage (W1–W5) | 4.44 | ≥ 4.2 | Design/local evidence meets the provisional bar; hosted/model proof remains open |
| Execution (X1–X5) | 4.02 | ≥ 4.2 | HOLD — reproducibility, hosted run, video, and performance evidence remain open |
| Potential Impact (I1–I5) | 3.50 | ≥ 4.0 | HOLD — no independent non-builder validation or measured impact |
| Creativity & Ambition (C1–C5) | 4.30 | ≥ 4.2 | Provisional local/design bar; public/video demonstration remains open |

The internal Tier A target is not met: multiple subcriteria are below 4, external gates are
open, and there is no final commit-bound manifest. The package makes no Tier A or E4 claim.
