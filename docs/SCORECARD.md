# Withheld internal scorecard

Snapshot: 2026-09-03, revised twice — after the page was published and the two hosted probes ran, and
again after four legibility repairs to the page and README that came out of a simulated review. This is
an internal 0–5 assessment, not an official judging result and not a Tier A claim. Scores reflect the
evidence class in the last column. A combined `VERIFIED_* + UNKNOWN` status means the local
design/run is real while the requested external proof is still open; `NOT_RUN` means the instrument
exists and is named but nobody has run it. The simulated review itself
(`docs/evidence/simulated-panel-2026-09-03.json`, `INFERENCE`) moves no score on its own: what moved
three of them is the page and README changes it prompted, which are in the source and in the 43/43
browser run.

| ID | Score | Current evidence | Design strength | Missing evidence | Failure condition | Next action | Evidence class |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| W1 Essentiality | 4.6 | Page-owned arithmetic/redaction in source; GATE-W1; 19 native checks locally and 19 again on the live URL | Same-page state is materially safer than an external grader | Model transcript and counterfactual proof; the hosted half is closed | Numeric, point, pass, identity, or release authority crosses the boundary | Run the model replay in `natural-language-replay-blocked.json` and rerun W1 | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W2 Tool quality | 4.7 | Nine closed tools, bounds, annotations, runtime validation, native registry captured locally and on the live URL; `manifest.json` binds both commits | Narrow tools expose useful recognition operations without a release tool | A model selecting from this registry in its own words | Extra fields, oversized payloads, or invalid IDs are silently accepted | Keep the registry stable and record the model's selection when replay runs | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W3 Context/authority | 4.6 | Revision guard, monotonic emphasis, atomic proposals, receipts, recovery trace; the stale-revision and duplicate-operation refusals also ran on the live URL | The page remains the authority for context and state transitions | Model-driven stale recovery; the hosted refusal capture is closed | Stale write overwrites current state or a human event is absent | Complete the model stale-retry journey | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W4 Shared state | 4.6 | UI and tools share `SessionPort`; browser trace observes DOM/revision/receipts locally and on the live URL | Agent work and human review converge on one visible state | The same DOM capture after a model's calls rather than a script's | Tool result comes from a mock or separate state | Capture before/after state during the model replay | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| W5 Safety/recovery | 4.0 | Injection quarantine, invalid/unknown/oversized/stale/duplicate refusals, no confirm tool, 27-check recovery | Refusal is bounded and recovery is actionable | Model behavior under the same refusals | Leak, agent release, hidden recovery, or unhandled exception | Run model safety repetitions and inspect refusal payloads | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X1 Complete loop | 4.2 | Local read → propose → hold → stage → human decline/confirm → reload trace, 27 checks; the live URL carries the same page states in 43 checks | The narrow journey is resettable and fully page-owned | The 27-check journey has run locally only, and no video exists | A judge must assemble the key sequence or final event is missing | Record the video from `docs/VIDEO-SCRIPT.md`; pointing the journey at the live URL needs a `--url` option `scripts/failure-recovery.mjs` does not have, and adding one moves `sourceSha256`, so it waits until after submission | `VERIFIED_ARTIFACT` + `NOT_RUN` |
| X2 Correctness | 4.5 | 136 tests; pure marks/session tests; browser and recovery assertions | Arithmetic and revision invariants are deterministic | Clean-clone and Node 22/CI evidence | Revision, mark, receipt, or release state diverges | Run fresh install and CI on a writable Node 22/24 runner | `VERIFIED_RUN` + `VERIFIED_ARTIFACT` + `ENVIRONMENT_BLOCKED` |
| X3 UX/demo clarity | 4.2 | Monochrome three-column UI; marked/staged views; visible refusal and human gate; the class's question is now on the screen, the cell strip has a key, the counters say what they are counted out of, and the empty call list says what would fill it | Agent role and human boundary are inspectable, and the four things a cold reader stumbled on are named on the page rather than in a document | Non-builder comprehension and final video | Dense first viewport or failure path is not understood | Run GATE-P2 protocol and record the judge path | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| X4 Responsiveness | 3.8 | 1440/1487/420 screenshots, focus/CSP/console and responsive checks, repeated against the live URL | Static local app has a useful no-agent fallback | Controlled latency, long-task, memory, real-device and manual checks | Tool/UI hangs, overflow, failed asset, or unexplained request | Run the performance probe on representative hardware and open the live URL on a physical phone | `VERIFIED_ARTIFACT` + `NOT_RUN` |
| X5 Reproducibility | 4.1 | Package scripts, MIT license, docs, source/build hashes; public repository `AndroLay/withheld` read anonymously; live URL HTTP 200 serving bytes identical to `dist/`; `manifest.json` binds `93eee30`, `b050f991` and this working commit, and predates the 18:02 republish to `9cce7d0a` / `15baf8f0` | A reviewer can clone the published repository and open the same page a judge opens | CI on a pinned runner, Node 22, and the video | Local-only instructions or hashes do not identify the release | Run CI/Node 22 on a writable runner; record the video from `docs/VIDEO-SCRIPT.md` | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `ENVIRONMENT_BLOCKED` |
| I1 Problem clarity | 3.8 | Short-answer marking problem and bounded fixture are documented; the question the class was set is on the screen above the queue, and README opens on the teacher's problem rather than on the mechanism | The problem is concrete rather than generic “AI for education”, and a reader can see what is being marked before reading a rubric line | GATE-P2 answers from a non-builder | Persona/problem is only author assertion or impact is overstated | Run the six-question GATE-P2 protocol | `SELF_REPORTED` + `VERIFIED_SOURCE` + `UNKNOWN` |
| I2 Audience specificity | 3.2 | Teacher/marker/reviewer wording and synthetic aliases | The intended workflow owner is understandable | Independent workflow-owner validation | No one outside the builder recognizes the use case | Recruit one eligible non-builder and preserve verbatim answers | `SELF_REPORTED` + `UNKNOWN` |
| I3 Before/after | 3.5 | Preview outcome and held subset show the intended state difference; README's first screen is a three-row before/after table whose right column is checkable in the live page | Recognition can reduce unattended work while preserving review, and the contrast with an ordinary assistant is stated before any mechanism | Measured or observed workflow comparison with a non-builder | No demonstrable difference from ordinary manual marking | Run a bounded before/after comprehension session; do not invent time savings | `VERIFIED_SOURCE` + `UNKNOWN` |
| I4 Human-agent split | 3.8 | Agent recognises/proposes; page computes/holds; human-only confirm; receipts | Authority is explicit and technically enforced | Natural-language model transcript and human validation | Agent computes marks, releases, or bypasses hold | Complete model replay and GATE-P2 | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| I5 Credibility/trust | 3.8 | Synthetic disclosure, no backend/network/PII, security and limitation docs; the live URL enforces the meta CSP from its own origin with 4 requests and none off-site | Claims are deliberately narrow and auditable | Independent review by a person who did not build it | Synthetic data presented as adoption or injection mitigation claimed as general | Obtain GATE-P2 and retain every limitation | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `SELF_REPORTED` + `UNKNOWN` |
| C1 Original mechanic | 4.6 | Information boundary, redacted payload, page arithmetic, human release; the boundary is re-swept on the live DOM by 17 checks | The memorable idea is a capability boundary, not a chatbot skin | The video's side-by-side demonstration | It looks like a generic AI grading dashboard | Show one refusal beside the page-owned arithmetic in the video | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `NOT_RUN` |
| C2 Human-agent relationship | 4.4 | “Recognizer, not grader” role, narrow tools, absent confirm tool | The agent has a precise seat and a deliberate refusal | Model-selected replay | Agent is framed as an autonomous grader or publisher | Replay natural-language requests and preserve tool selection | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |
| C3 WebMCP novelty | 4.4 | Native registry and page-local state/permission checks, captured on the live URL as well as locally | WebMCP is used for same-page context, not as decoration | The counterfactual narrated aloud, and a model doing the calling | A conventional external API provides the same experience | Narrate the counterfactual in the video; record the model replay | `VERIFIED_ARTIFACT` + `UNKNOWN` |
| C4 Differentiation | 4.4 | Distinct marking/release thesis and bounded static scope; published as its own standalone repository and URL, separate from every other workspace package | The project has a clear domain and does not need feature sprawl | The Devpost entry itself | It is confused with another workspace submission or benchmark | Paste `docs/SUBMISSION-TEXT-WINNER-STYLE-DRAFT.md` when the owner files the form | `SELF_REPORTED` + `VERIFIED_SOURCE` + `UNKNOWN` |
| C5 Ambition/scope | 4.1 | Deep trust-boundary design completed in a small prototype; recovery exists; delivery closed without adding scope | Scope is ambitious in safety while remaining buildable | Model replay, GATE-P2 and the video | Speculative backend/features displace evidence closure | Finish the three remaining external gates before adding scope | `VERIFIED_SOURCE` + `VERIFIED_ARTIFACT` + `UNKNOWN` |

## Aggregate and target check

| Dimension | Mean | Internal target | Result |
| --- | ---: | ---: | --- |
| WebMCP Leverage (W1–W5) | 4.50 | ≥ 4.2 | Meets our own provisional bar — self-set, not a judge's — on design, local runs and the live URL; model proof remains open |
| Execution (X1–X5) | 4.16 | ≥ 4.2 | HOLD — the video, CI/Node 22 and the performance baseline remain open |
| Potential Impact (I1–I5) | 3.62 | ≥ 4.0 | HOLD — still the lowest dimension. Two subcriteria moved on 2026-09-03 because the artefact changed, not because anything was validated: there is no independent non-builder validation and no measured impact. Publishing did not move this dimension, and neither did the simulated review |
| Creativity & Ambition (C1–C5) | 4.38 | ≥ 4.2 | Meets our own provisional bar; the video demonstration remains open |

Publishing on 2026-09-03 moved ten subcriteria and none of them in Potential Impact: W2 4.6→4.7,
W3 4.5→4.6, W4 4.5→4.6, X4 3.7→3.8, X5 3.6→4.1, I5 3.7→3.8, C1 4.5→4.6, C3 4.3→4.4, C4 4.3→4.4,
C5 4.0→4.1. Each increment names the artefact that closed it.

The four legibility repairs later the same day moved three more: I1 3.6→3.8, I3 3.2→3.5, X3 4.1→4.2.
Those three are increments for what a reader can now see on the page and in README's first screen —
the question, the strip key, the counters' denominator, the empty call list, and the before/after table
— and each is checkable in `src/` and in the 43/43 browser run. They are **not** credit for the
simulated review that prompted them, which is `INFERENCE` and closes nothing. I2, I4 and I5 are
unchanged and I2 is still the lowest score in the sheet, because the thing it waits on is a person.

The internal Tier A target is not met: six subcriteria are still below 4, three external gates are
open — the natural-language model replay, GATE-P2 and the video — and the package makes no Tier A or
E4 claim.
